# 정책 정의서 — Yahoo_Prompt (의료질문/문진 카테고리)

> 문서: 05-policy.md · 상태: 초안 · 열린 질문 [?] 0개 · 채택한 [제안] 0개 · 마지막 갱신: 2026-09-14(월)
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.

F-의료1(의료질문/문진 신호 추출, 04-features.md)에 대한 고정 규칙 — ~~`draft/schemas/의료질문.md`~~(QA 감사 발견: 이 파일은 더 이상 존재하지 않음 — 정본 경로가 `C:\AlJalTtack\prd\의료질문.md`로 이전됨) `prd/의료질문.md`(우석민) 번역. 500자 상한·3회 소프트 넛지·겹침 처리 규칙(P-공22) 등 카테고리 공통 정책은 `docs/docs-공통/05-policy.md`(P-공1~P-공22, SP-공1~SP-공2)에 이미 있으므로 다루지 않는다. 행위자 표기는 03·04와 동일하게 A-공1 기준으로 통일했다(정책 표의 "누가"는 전부 시스템 내부 규칙이라 행위자 표기가 직접 등장하지는 않음).

**[QA 감사 발견, 신규 반영]** `docs/docs-공통/05-policy.md`의 **P-공26**·**P-공30**은 의료질문 카테고리를 직접 지목한 이미 팀 확정된 사항인데, 이 문서(및 03·04·06)에 그동안 반영되어 있지 않았다. 아래 서비스 정책 표에 원문을 인용해 반영한다.

## 정책

| P | 대상 F | 누가 | 언제(상태값) | 규칙 (할 수 없다 · 까지) | 위반 시 앱이 보이는 것 |
|---|---|---|---|---|---|
| P-의료1 | F-의료1 | 시스템 | 축1(목적) 신호가 하나도 감지되지 않은 시점 | 신호가 없다고 에러로 처리할 수 없다 — 반드시 기본값("증상·질병 정보 알아보기")으로 채워야 한다 | 기본값이 적용된 프롬프트가 정상적으로 표시된다(에러 화면 없음) |
| P-의료2 | F-의료1 | 시스템 | 축2(대상) 신호가 하나도 감지되지 않은 시점 | 신호가 없다고 에러로 처리할 수 없다 — 반드시 기본값("본인")으로 채워야 한다 | 기본값이 적용된 프롬프트가 정상적으로 표시된다(에러 화면 없음) |
| P-의료3 | F-의료1 | 시스템 | 축3(증상 성격) 신호가 하나도 감지되지 않은 시점 | 신호가 없다고 에러로 처리할 수 없다 — 반드시 기본값("갑자기 생긴 증상")으로 채워야 한다 | 기본값이 적용된 프롬프트가 정상적으로 표시된다(에러 화면 없음) |
| P-의료4 | F-의료1 | 시스템 | 항상, 생성되는 프롬프트/응답 어디든 | "이 정보는 진단을 대체하지 않으며, 응급 증상은 즉시 119/응급실로 문의하세요" 안전 문구를 항상 고정 삽입해야 한다(생략 불가) | (시스템 내부 규칙이라 위반 화면 없음 — 안전 문구 누락 시 버그로 처리) |

## 상태값

F-의료1에는 별도의 상태값 전이가 없음 — 축1/축2/축3은 매 요청마다 문장에서 새로 추출되는 값이며 저장되는 상태가 아님(hibro F-상담1과 같은 성격).

## 서비스 정책

| SP | 한 줄 |
|---|---|
| (이 카테고리 로컬 SP 없음) | 겹침 처리 규칙은 **공통 정책으로 승격됨** — `docs/docs-공통/05-policy.md` 의 **P-공22** 참고. 5개 카테고리 스키마 전부에 동일 문구가 확인되어 카테고리 로컬 판단 근거가 모두 모였으므로, 이 문서에 있던 SP1(겹침 처리, 로컬)은 삭제하고 공통 참조로 대체함 |
| (이 카테고리 로컬 SP 없음) | **[QA 감사 발견, 신규 반영]** `docs/docs-공통/05-policy.md`의 **P-공26**(신규, 안건2 — 프로젝트 리드 최종 결정) 참고: "의료질문·개인상담(민감정보/개인 고민 노출 위험이 큰 2개 카테고리)은 원본 입력 문장을 저장하지 않는다. 축(axis) 값 + 만족도 평점(👍/👎)만 저장한다." 이 카테고리(의료질문)가 원문에서 직접 지목된 대상이다. **이미 코드로 구현 완료된 상태** — `C:\AlJalTtack\api\feedback.ts`의 `NO_RAW_TEXT_CATEGORIES`에 반영되어 있음(QA 감사 확인) |
| (이 카테고리 로컬 SP 없음) | **[QA 감사 발견, 신규 반영]** `docs/docs-공통/05-policy.md`의 **P-공30**(신규, 안건3 — 팀 확정, 해소됨) 참고: "의료질문·개인상담 카테고리 예시를 작성하는 시점 — 안전문구·면책 문구를 예시에 별도로 붙이지 않는다." 이 카테고리(의료질문)가 원문에서 직접 지목된 대상이다. (참고: F-의료1 자체의 프롬프트/응답에 안전 문구를 항상 고정 삽입하는 규칙은 이것과 별개인 **P-의료4**이며, 예시 문장 작성 규칙(P-공30)과 혼동하지 않는다) |

## 검토했으나 제외
| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
|---|---|---|
| (없음) | | |
