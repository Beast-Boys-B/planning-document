> 문서: 05-policy.md · 상태: 초안 · 열린 질문 [?] 19개 · 채택한 [제안] 8개 · 마지막 갱신: 화
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.

## 정책
| P | 대상 F | 누가 | 언제(상태값) | 규칙 (할 수 없다 · 까지) | 위반 시 앱이 보이는 것 |
|---|---|---|---|---|---|
| P1 | F1 | 모든 방문자(제약 없음) | 무관함(로그인 전/후·서비스 점검 등 특정 상태와 무관하게 항상 가능) | 카테고리를 선택하지 않으면 다음 단계로 진행할 수 없다 | 다음 단계로 넘어가지지 않는다 |
| P2 | F2 | A1(사용자) | [?] | 입력이 비어 있으면 프롬프트 생성을 실행할 수 없다 | [프롬프트 만들기] 버튼이 눌리지 않는다 |
| P3 | F3 | A1(사용자) | [?] | 신호가 하나도 감지되지 않아도 에러로 처리할 수 없다(반드시 기본값으로 채워 결과를 내야 한다) | 해당 없음 — 기본값으로 자동 채워진 정제 프롬프트가 정상적으로 표시된다(에러 화면 없음) |
| P4 | F4 | A1(사용자) | [?] | 정제 결과가 원본과 완전히 같아도 비교 화면 표시를 생략할 수 없다 | 원본과 정제본이 동일하다는 안내 문구가 표시된다 |
| P5 | F5 | A1(사용자) | [?] | 톤·길이·축 토글 조정은 제한 없이 언제든 가능하다(할 수 없는 경우 없음) | 해당 없음 |
| P6 | F6 | A1(사용자) | [?] | 재조정 시 자연어를 다시 입력하지 않고는 못 돌아가게 막을 수 없다(항상 토글 화면으로 바로 이동 가능해야 한다) | 해당 없음 |
| P7 | F1 | [?] | [?] | 카테고리는 한 번에 하나만 선택할 수 있다(복수 선택 불가) | 이미 선택된 카테고리가 있는 상태에서 다른 카테고리를 누르면, 기존 선택이 해제되고 새 카테고리로 교체된다 |
| P8 | F2 | [?] | [?] | 자연어 입력은 최대 500자까지만 허용한다 (참고: 여행계획처럼 여러 축 신호를 한 문장에 담아야 하는 카테고리도 있어서 여유 있게 잡음) | 500자를 넘으면 초과 안내 문구가 뜨고 [프롬프트 만들기] 버튼이 비활성화된다 |
| P9-A | F2 | [?] | [?] | 욕설 키워드 블랙리스트에 걸리는 입력은 그대로 처리할 수 없다 | 블랙리스트 키워드가 포함된 부분에 경고 문구가 표시되거나 마스킹 처리된다 |
| P10 | F3 | [?] | [?] | 여러 축(목적·톤·길이 등)에서 신호가 서로 충돌하면 임의로 아무거나 적용할 수 없다(정해진 우선순위대로 적용해야 한다) | 충돌 시 우선순위가 가장 높은 축의 값이 적용되고, 나머지는 기본값으로 채워진다 |
| P11 | F5 | [?] | [?] | 여러 토글을 동시에 조정해도 값이 서로 어긋난 채로 반영될 수 없다(마지막 변경 기준으로 항상 하나의 일관된 정제 프롬프트여야 한다) | 토글 변경 즉시 최신 값 기준으로 프롬프트가 재계산되어 갱신된다 |
| P12 | F6 | [?] | [?] | 3회 이상 조정하면 도움말(소프트 넛지)을 띄운다 | 도움말(소프트 넛지) 팝업이 표시된다(제한이 아니라 안내이므로 조정 자체는 계속 가능) |
| P13 | F7 | [?] | [?] | 클립보드 복사가 실패한 채로 "복사됨"이라고 보여줄 수 없다 | 복사 실패 시 실패 안내와 함께 수동으로 선택해 복사할 수 있는 텍스트 영역이 표시된다 |

> 참고: P2~P6 의 "누가"가 A1(사용자)로 통일된 이유 — F2~F6이 전부 사용자가 화면을 조작하는 순간에 발생하는 예외라 A2(운영자)가 낄 자리가 없고, 로그인 개념도 없어 다른 조건도 고려하지 않음 (팀 결정 사유)

## 상태값
카테고리 선택 전 → 카테고리 선택됨 → 프롬프트 정제 완료 → 재조정 중 → (P12 재조정 상한 도달 시) 대안 안내 · 전이 조건: 각 단계는 이전 단계의 결과가 확정돼야 다음으로 넘어가며, 재조정 상한 도달 시에만 "대안 안내" 상태로 전환

> 주의: 위 상태값은 P12 가 원래 "하드 리밋(최대 반복 횟수)" 안이었을 때 함께 확정된 것인데, P12 가 이후 "3회 이상 조정 시 소프트 넛지(도움말)"로 바뀌면서 "재조정 상한 도달 시 대안 안내로 전환"이라는 전이 조건과 서로 안 맞을 수 있음 — 팀이 그대로 둘지 고칠지 다음 질문에서 확인 필요.

## 서비스 정책
| SP | 한 줄 |
|---|---|
| SP1 | MVP 단계에서는 로그인·계정 없이 방문 세션 내에서만 정제 결과가 유지된다(기록 저장·불러오기는 F9 로 v2에서 다룸) |

## 보류(v2)
| 후보 | 출처(제안 근거) | 보류 사유 |
|---|---|---|
| P9-B (개인정보 감지): 개인상담·의료 등 민감한 내용(전화번호·주민번호 등)이 포함된 입력에 대한 처리 | [제안] — 근거: 02-workflow.md A1 한 줄 상황 "개인상담이나 의료분야 질문 등의 목적으로 쓰는 경우도 있는데" | 지명 같은 자유 슬롯 문제와 똑같이 규칙 기반으로 못 잡는 영역이라 v2로 미룸 (팀 결정) |

## 검토했으나 제외
| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
|---|---|---|
| P12(원안) 재조정 루프(F5↔F6) 하드 리밋 — 최대 반복 횟수 이후 강제로 막고 대안 안내 화면을 띄움 | [제안] — 근거: 02-workflow.md S5·S6 불편 "감으로 시행착오를 반복" · "포기" | 재조정 루프를 줄이는 게 이 프로젝트의 핵심 가치라 하드 리밋(횟수 제한)을 걸면 설계 의도와 충돌함 (팀 결정) |
