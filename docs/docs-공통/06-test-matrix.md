> 문서: 06-test-matrix.md (공통/통용 규칙 — 카테고리 무관) · 상태: 초안 · 열린 질문 [?] 9개(TC-공9 해소로 11개→10개, TC-공5 선택지 문구 확정으로 10개→9개) · 채택한 [제안] 1개(제안 4개 제시 → 1개는 거절/대체(TC-공10, 프로젝트 리드 확정으로 해소), **1개 채택(TC-공5 객관식 선택지 5개 문구, 프로젝트 리드 확정 2026-09-15)**, 2개 팀 채택 대기) · **TC-공10(👍/👎 중복 클릭) 해소 — 프로젝트 리드가 원래 설계 의도 전달, P-공23·F-공20 동시 갱신** · **TC-공5(3회 재조정 시 객관식 확인) 구현 완료 반영** — AlJalTtack 리포 커밋 `ed7ec99`, main 배포 완료. ~~객관식 선택지 5개는 `[제안]`(팀 미확정)~~ → **[프로젝트 리드 확정, 2026-09-15]** 선택지 5개 문구 그대로 채택, × 버튼 닫기는 Claude UX 판단(팀 확인 필요 `[?]`, 이번 확정 범위 아님) · **TC-공9(클립보드 복사 실패) 구현 완료 반영(QA 감사, 2026-09-14)** — AlJalTtack 리포 커밋 `2310bea`, main 배포 완료. `ComparePage.tsx`에 `copyFailed` 상태 + 읽기전용 textarea(수동 복사 영역)로 구현 완료, 미해결 논점 `[?]` 해소 · 마지막 갱신: 2026-09-15(화)
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
>
> 이 문서는 03-requirements.md·04-features.md·05-policy.md 에 이미 있는 답을 "테스트 케이스/엣지케이스" 관점으로 다시 정리한 QA용 매트릭스다. **카테고리 무관 공통 케이스만** 다룬다 — 카테고리별 엣지케이스(의료질문·개인상담·여행계획·작성·AI사진생성 각각의 축·신호 관련 케이스)는 각 카테고리 폴더에 별도 문서로 만들어질 예정이라 여기서 다루지 않는다. 이미 03/04/05에 답이 있는 케이스는 근거 열에 원문 출처를 밝히고 "기대 동작"에 그 내용을 그대로 옮겨 적었다(지어내지 않음). 답이 없는 진짜 새로운 케이스는 "기대 동작"에 `[?]`를 쓰고, 필요하면 `[제안]`으로 후보를 하나 보탰다 — 팀 대신 정하지 않았다.

## 테스트 케이스

| 케이스 | 예시 입력/상황 | 기대 동작 | 근거(R/F/P, 있으면) | 확인 필요 |
|---|---|---|---|---|
| TC-공1 빈 입력으로 "프롬프트 만들기" 실행 시도 | 자연어 입력란이 빈 채로 [프롬프트 만들기]/[분석] 버튼을 누름 | 버튼이 눌리지 않는다(다음 단계로 진행 불가). 미입력 상태로 진행 시도 시 안내 문구 표시 | F-공2 예외("입력이 비어 있으면 버튼이 눌리지 않는다"), P-공2("입력이 비어 있으면 프롬프트 생성을 실행할 수 없다" / "[프롬프트 만들기] 버튼이 눌리지 않는다"), F-공11 예외("입력 내용이 없는 상태에서 '분석' 버튼을 누르면 안내 메시지를 표시하고 진행하지 않는다") | — |
| TC-공2 500자 상한 초과 입력 | 501자 이상 입력 시도 | 500자를 넘으면 초과 안내 문구가 뜨고 버튼이 비활성화된다(500자 상한 확정) | P-공8(woomin P8, 500자 최종 확정), P-공17(yeonsak P2, 같은 결정으로 확정) | — |
| TC-공3 카테고리 미선택 상태에서 진행 시도 | 카테고리 버튼 5개 중 아무것도 누르지 않고 다음 단계로 진행 시도 | 다음 단계로 넘어가지지 않는다. 카테고리 선택 안내 문구 표시 | P-공1("카테고리를 선택하지 않으면 다음 단계로 진행할 수 없다" / "다음 단계로 넘어가지지 않는다"), F-공1 예외("카테고리를 안 고르면 다음 단계로 못 넘어감"), F-공10 예외("카테고리를 선택하지 않은 채 다음 단계로 진행하려 하면 '카테고리를 선택해주세요' 안내 메시지") | — |
| TC-공4 모든 축에서 신호가 하나도 감지되지 않는 완전 중립적 문장 | 예: "오늘 날씨가 좋네요"처럼 어떤 카테고리 축에도 걸리지 않는 문장 | 에러로 처리하지 않는다. 신호가 하나도 감지되지 않아도 전부 기본값으로 자동 채워진 정제 프롬프트가 정상적으로 표시된다(에러 화면 없음) | F-공3 예외("신호가 하나도 감지 안 되면 전부 기본값으로 채워진다(에러 아님)"), P-공3("신호가 하나도 감지되지 않아도 에러로 처리할 수 없다(반드시 기본값으로 채워야 한다)") | — |
| TC-공5 [복사] 후 재조정 사이클이 3회 이상 반복 | [복사] 클릭 후 축 토글이나 원본 텍스트를 다시 수정하는 것을 3회 이상 반복 | 3회 누적 시점에 "축 조정이 결과에 미치는 영향"을 예시로 보여주는 도움말(소프트 넛지)이 표시된다. 강제 종료·상한이 아니라 이후에도 계속 조정 가능하다. 같은 시점에 선택지(객관식) 형태의 "어떤 점이 어려우셨나요" 확인도 함께 노출된다. **[구현 완료, AlJalTtack 리포 커밋 `ed7ec99`, main 배포 완료, 2026-09-14]** `ComparePage.tsx`에 실제 반영됨 — 힌트 카드와 "조정을 몇 번 반복하셨네요. 어떤 점이 어려우셨나요?" 객관식 카드가 함께 뜨고, 선택지 하나를 고르면 즉시 전송·잠금(재클릭 불가, P-공23/F-공20과 동일 패턴), 우상단 × 버튼으로 언제든 닫을 수 있음(비강제). 응답은 Neon `difficulty_survey` 테이블에 `api/survey.ts`로 익명 저장(R-공26 기조와 동일). ~~**[제안]** 객관식 선택지 5개(코드 `SURVEY_REASONS`): "원하는 톤·분위기가 안 나와요" / "핵심 내용이 자꾸 빠져요" / "축(옵션) 의미가 헷갈려요" / "결과가 너무 길거나 짧아요" / "기타" — 근거: 재조정이 반복되는 흔한 마찰 지점(톤·누락·축 의미·길이)을 커버하려 한 추정, 특정 카테고리 조사·데이터 근거는 없음~~ → **[프로젝트 리드 확정, 2026-09-15]** 위 선택지 5개 문구 그대로 확정, 코드 변경 없음 | P-공12(카운트 기준="[복사] 후 재조정 사이클" 3회, 프로젝트 리드 확정), P-공24(선택지 형태 확정), F-공21(구현 완료, 커밋 `ed7ec99`) | — (하드 리밋 방식은 05 검토했으나 제외 표에서 이미 팀이 거절함: "재조정 루프를 줄이는 게 핵심 가치라 하드 리밋을 걸면 설계 의도와 충돌") · **[신규]** × 버튼으로 언제든 닫는 비강제 방식은 문서에 팀이 정한 적 없는 것을 이번에 Claude가 UX 판단으로 구현한 것 — 팀 확인 필요 `[?]` · ~~**[신규]** 객관식 선택지 5개 문구는 `[제안]`이라 팀 검토·채택 여부 확인 필요 `[?]`(문구 변경 가능)~~ → **[프로젝트 리드 확정, 2026-09-15]** 선택지 5개 문구 확정, 해소됨 |
| TC-공6 욕설·비속어가 포함된 입력 | 블랙리스트에 걸리는 욕설 단어를 포함해 제출 | 블랙리스트 키워드에 걸리는 입력은 그대로 처리할 수 없다. 블랙리스트 키워드 부분에 경고·마스킹 처리가 적용된다 | P-공9(P9-A, woomin, "욕설 키워드 블랙리스트에 걸리는 입력은 그대로 처리할 수 없다" / "블랙리스트 키워드 부분에 경고·마스킹 처리") | 마스킹의 정확한 표시 방식(전체 차단 vs 단어 일부만 가림 등 구체적 UI)은 05에 명시가 없다 → `[?]` |
| TC-공7 한국어가 아닌 언어(영어 등) 전용 입력, 또는 한/영 혼용 입력 | 예: "Please write an email to my boss", "이메일 write 부탁해" | `[?]` — 04-features.md F-공12(yeonsak F3) 비고에 "여러 언어·오탈자·은어로 키워드 매칭이 안 되는 입력 처리 방식은 [?]"로 이미 미해결 논점으로 명시되어 있다. 팀이 논의한 적이 없는 것으로 보인다 | F-공12 비고("신규 발견 [?]: 분석 엔진 오류·지연, 다국어/오탈자 처리는 woomin 문서엔 아예 없던 논점"), F-공11 예외("입력이 지나치게 길 경우… 처리 방식·상한선은 [?]"과 인접 논점) | 다국어/한영혼용 입력을 TC-공4(중립 문장)와 동일하게 "신호 미검출→기본값"으로 처리할지, 별도 안내(예: "한국어로 입력해주세요")를 줄지 팀 결정 필요 `[?]` · `[제안]` 별도 로직 없이 F-공3/P-공3의 "신호 없음→기본값 처리, 에러 아님" 원칙을 그대로 재사용하는 안 — 근거: 이미 확정된 원칙을 최소 변경으로 재적용 가능하다는 점(F-공3/P-공3) |
| TC-공8 숫자·특수문자만 있거나 의미를 알 수 없는 입력 | 예: "!!!1234$$$" | TC-공4와 같은 원리로 신호 미검출 → 기본값으로 자동 채워진 정제 프롬프트가 표시된다(에러 아님)로 볼 수 있다. 단 03/04/05는 "신호 검출 여부"만 기준으로 삼고, 입력 자체의 "의미 있음 여부"를 별도로 판별하는 로직은 언급하지 않는다 | F-공3 예외, P-공3(신호 없음→기본값 원칙 재적용) | 이런 입력을 TC-공1(빈 입력)에 준하는 것으로 별도 취급(예: 별도 경고나 재입력 요청)할지, 아니면 그냥 "신호 없음"으로 넘어갈지는 명시적으로 논의된 바 없다 `[?]` |
| TC-공9 클립보드 복사 실패(브라우저 권한 거부 등 기술적 실패) [구현 완료] | [복사] 클릭 시 브라우저가 클립보드 접근 권한을 거부해 복사가 실패함 | (일반 실패 케이스는 확정) 복사 실패 안내 + 수동 복사 영역이 표시된다. **[구현 완료, AlJalTtack 리포 커밋 `2310bea`, main 배포 완료, 2026-09-14]** `ComparePage.tsx`에 실제 반영됨 — `navigator.clipboard.writeText().catch()`에서 `copyFailed` 상태를 true로 전환, "⚠ 자동 복사에 실패했어요. 아래 내용을 직접 선택해서 복사해주세요." 안내 문구와 함께 `readOnly` textarea(포커스 시 자동 select되는 수동 복사 영역)가 노출된다. "권한 거부"라는 특정 원인을 그 외 실패 원인과 구분하지 않고 P-공13/P-공19와 동일한 하나의 "수동 복사 영역" 방식으로 처리 | P-공13(woomin P13, "클립보드 복사 실패 상태로 '복사됨' 표시 불가" / "복사 실패 안내 + 수동 복사 영역 표시"), P-공19(yeonsak P4, "복사 실패 시… 안내") | ~~04-features.md F-공13(yeonsak F4) 비고에 "브라우저 클립보드 접근 권한이 차단된 경우의 처리 방식은 [?]"로 이미 명시된 미해결 논점 — "권한 거부"라는 특정 원인에도 P-공13과 똑같이 "수동 복사 영역"만 보여주면 되는지, 아니면 권한 허용 방법을 안내하는 별도 문구가 필요한지는 `[?]`~~ → **해소됨** — 권한 허용 방법을 안내하는 별도 문구는 만들지 않고, P-공13과 동일한 "수동 복사 영역"만 보여주는 방식으로 구현 완료 |
| TC-공10 👍/👎 피드백 버튼 중복 클릭 | 👍를 누른 뒤 다시 👍를 누르거나, 👍를 누른 뒤 👎로 바꿔 누름 | **[프로젝트 리드 확정, 해소됨]** 만족도 평가는 프롬프트 완성(페이지2 세션)당 정확히 1회만 허용된다 — 자연어를 직접 입력했든 예시 문장을 선택했든 편집 방식과 무관하게, 하나의 세션 동안 👍 또는 👎를 처음 클릭하면 그 값으로 전송되고 이후 두 버튼 모두 비활성화(잠금)된다. 토글 취소도, 값을 바꿔 다시 클릭하는 것도 불가능하다("첫 클릭 후 잠금" 방식, "최신값 덮어쓰기" 방식 아님). 이미 코드에도 반영 완료(`ComparePage.tsx` `feedbackGiven` 상태로 첫 클릭 후 두 버튼 disabled 처리, 커밋 a786a24, main 배포 완료) — 문서가 뒤늦게 실제 결정을 따라잡음 | P-공23(갱신), F-공20(갱신) | ~~중복·번복 클릭 처리 방식 `[?]`~~ → 해소됨. ~~`[제안]` P-공11 재사용해 마지막 클릭 값으로 덮어쓰는 방식~~ → **채택되지 않음**(프로젝트 리드가 원래부터 "세션당 1회 + 첫 클릭 후 잠금"으로 의도했음을 확인, 덮어쓰기 방식으로 대체된 것이 아니라 애초에 이 방식이 맞았음) |
| TC-공11 피드백 저장 시 서버/네트워크 오류 | 👍/👎 클릭 시 서버 응답이 없거나 네트워크가 끊김 | `[?]` — 03/04/05 어디에도 피드백 저장 실패에 대한 처리가 언급되어 있지 않다 | 없음(신규 발견 논점) | 저장 실패 시 사용자에게 오류를 보여줄지, 조용히 무시할지 `[?]` · `[제안]` F-공24 예외("로컬 저장소 접근 불가 시 에러 안내 없이 조용히 개인화 없이 진행")와 같은 서비스 톤을 재사용해, 저장 실패 시에도 사용자에게는 그대로 감사 문구를 보여주고 조용히 재시도/드롭하는 안 — 근거: F-공24에서 이미 "실패를 사용자에게 노출하지 않는다"는 방향이 확인됨 |
| TC-공12 카테고리를 전환할 때 이미 입력해둔 문장을 유지할지 초기화할지 | 페이지2에서 [뒤로]를 눌러 페이지1로 돌아가 다른 카테고리를 고르는 경우 | (뒤로 경유 케이스는 확정) 기존 자연어 입력은 유지된 채 카테고리만 다시 선택할 수 있다. 자동 선택 옵션은 초기화 후 재분석되어 새 결과가 표시된다 | F-공15(yeonsak F6, "새 카테고리 선택 시 기존 자연어 입력은 유지, 자동 선택 옵션은 초기화 후 재분석하여… 새 결과 표시"), 04-features.md 화면 구성 표 페이지2 비고("[뒤로](페이지1로 이동, 자연어 입력은 유지한 채 카테고리만 다시 선택)") | 페이지1 자체에서(뒤로 경유 없이) 카테고리 버튼만 다시 눌러 다른 카테고리로 바로 전환하는 경우에도 입력이 똑같이 유지되는지는 명시가 없다 `[?]` |
| TC-공13 신호 분석 엔진 자체 오류·지연 | 신호 추출 로직이 타임아웃되거나 내부 오류로 응답하지 않음 | `[?]` — 04-features.md F-공12(yeonsak F3) 비고에 "분석 엔진 자체 오류·지연 시 처리 방식은 [?]"로 이미 명시된 미해결 논점(TC-공4·8의 "신호 없음"과는 다른 논점 — 이건 엔진 자체가 죽거나 응답이 없는 경우) | F-공12 비고 | 재시도 UI, 타임아웃 안내 문구, 최대 대기 시간 등 `[?]` |
| TC-공14 재조정/재분석 요청을 연속으로 여러 번 클릭(연타) | [프롬프트 생성]/[분석] 버튼을 빠르게 여러 번 누름 | `[?]` — 05-policy.md P-공18(yeonsak P3) 비고에 "재분석 요청을 연속으로 여러 번 보낼 때 처리 순서는 [?]"로 이미 명시된 미해결 논점 | P-공18 비고 | 마지막 요청만 반영할지, 요청을 큐잉할지, 처리 중 버튼을 일시 비활성화할지 `[?]` |
| TC-공15 외부 AI 서비스 바로가기 버튼 클릭 시 새 탭이 차단됨(팝업 차단) | 브라우저 팝업 차단 설정으로 새 탭이 열리지 않음 | `[?]` — 04-features.md F-공25 예외에 "새 탭이 차단되거나 열리지 않는 경우 처리는 [?]"로 이미 명시된 미해결 논점 | F-공25 예외 | 팝업 차단 안내 문구가 필요한지, 필요하다면 어떤 문구인지 `[?]` |
| TC-공16 브라우저 로컬 저장소 접근 불가(개인화 기능) | 시크릿 모드 등으로 로컬 저장소 접근이 차단된 상태에서 재방문 | 에러 안내 없이 조용히 개인화 없이 진행한다(별도 폴백 로직 없음, 팀 확정) | F-공24 예외("로컬 저장소 접근 불가 시 에러 안내 없이 조용히 개인화 없이 진행 — 별도 폴백 로직 없음") | — |
| TC-공17 동일 사용자가 여러 탭·창에서 동시에 사용 | 두 개의 브라우저 탭에서 서로 다른 카테고리·입력으로 동시에 작업 | 각 탭·창은 서로 완전히 독립된 세션 상태를 가진다(탭 간 상태 공유 없음) — 한 탭의 진행이 다른 탭에 영향을 주지 않으며, 충돌 자체가 구조적으로 발생하지 않는다 | P-공16(yeonsak P8, 팀 확정) | — |

## 검토했으나 제외
| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
|---|---|---|
| (해당 없음 — 이 문서는 초안 → 팀 결정 목록을 종합한 것이 아니라 03/04/05를 읽고 새로 작성한 첫 버전이라, 아직 팀이 거절한 후보가 없다) | — | — |
