# 테스트 케이스/엣지케이스 매트릭스 — Yahoo_Prompt (AI 사진생성 카테고리)

> 문서: 06-test-matrix.md · 상태: 초안 · 열린 질문 [?] 2개(T-사진7 축1 좌우 동시 희망 문장의 단일선택 처리 적정성, T-사진8 태그 조립 순서 고정 여부) · 채택한 [제안] 0개 · 마지막 갱신: 2026-09-14(월)
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.

이 문서는 **AI사진생성 카테고리 고유의**, 3개 전용 축(스타일/피사체/구도)과 고정 규칙 2개에서 나오는 엣지케이스만 다룬다. 빈 입력·500자 초과·욕설 등 5개 카테고리 공통 엣지케이스는 `docs/docs-공통/06-test-matrix.md`에서 다루며 이 문서에서는 다루지 않는다.

케이스는 `draft/schemas/AI사진생성.md`, `docs/docs-AI사진생성/{03-requirements,04-features,05-policy}.md`, `예시-라이브러리.md`를 근거로 도출했다. 스키마·정책에서 이미 확정된 동작은 확정 행으로 적었고, 문서 어디에도 규정이 없는 것은 `[?]`로 남겼다.

## 매트릭스

| 케이스 | 예시 입력 | 기대 동작 | 근거(스키마/R/F/P) | 확인 필요 |
|---|---|---|---|---|
| T-사진1 — 축1(스타일) 좌/우 신호 동시 등장 | "사진처럼 사실적인데 만화 같은 느낌도 나게 그려줘" (좌 신호 "사진"·"사실적" 먼저 등장, 우 신호 "만화" 나중 등장) | 겹침 처리 규칙에 따라 더 나중에 등장한 신호(우: 일러스트/만화/추상적) 채택 | schemas/AI사진생성.md 겹침 처리 규칙(35~37행) · docs-공통 05-policy.md P-공22(공통 정책 승격 확정) | (없음 — 이미 확정된 규칙) |
| T-사진2 — 축2(주요 피사체) 좌/우 신호 동시 등장 | "인물이 있는 풍경 사진을 만들어줘" (좌 신호 "인물" 먼저, 우 신호 "풍경" 나중) | 더 나중에 등장한 신호(우: 인물 중심 아님) 채택 | schemas/AI사진생성.md 겹침 처리 규칙(35~37행) | (없음 — 이미 확정된 규칙) |
| T-사진3 — 축3(구도/프레이밍) 좌/우 신호 동시 등장 | "클로즈업으로 얼굴을 보여주다가 배경까지 넓게 다 보이게 해줘" (좌 신호 "클로즈업" 먼저, 우 신호 "넓게" 나중) | 더 나중에 등장한 신호(우: 광활한 뷰) 채택 | schemas/AI사진생성.md 겹침 처리 규칙(35~37행) | (없음 — 이미 확정된 규칙) |
| T-사진4 — 3개 축 모두 신호 없는 완전 중립 문장 | "그냥 예쁘게 만들어줘" (스타일·피사체·구도 신호 키워드 전부 없음) | 에러 처리 없이 3개 축 전부 기본값 태그로 채워짐: 스타일=사진 같은 현실적, 피사체=인물 중심, 구도=광활한 뷰 | schemas/AI사진생성.md 기본값 3곳(12·18·24행) · P-사진3(05-policy.md 14행) | (없음 — 이미 확정된 규칙) |
| T-사진5 — 고정 규칙1(저작권 안내) 조건 없이 항상 표시 | "그냥 개인 소장용으로 강아지 사진 하나 그려줘" (상업 의도 없음) | 용도 판단과 무관하게 결과 확인 화면에 저작권·이용약관 확인 안내 문구가 예외 없이 항상 표시됨 | R-사진2(03-requirements.md 13행, 갱신) · F-사진3(04-features.md 14행, 조연익 확정) · P-사진1(05-policy.md 12행) | (없음 — 조연익 확정, 다시 묻지 않음) |
| T-사진6 — 고정 규칙2(인물 추가정보 팁) 조건 없이 항상 표시 | "풍경 사진을 만들어줘" (피사체=인물 아님, 축2 우 판정) | 피사체 판정과 무관하게 인물의 나이대·성별·표정·스타일 추가 입력을 권장하는 안내 문구가 예외 없이 항상 표시됨 | R-사진5(03-requirements.md 16행, 갱신) · F-사진4(04-features.md 15행, 조연익 확정) · P-사진2(05-policy.md 13행) | (없음 — 조연익 확정, 다시 묻지 않음) |
| T-사진7 — 축1(스타일) 좌/우가 동시에 "원해지는" 문장 | "사진처럼 사실적인데 약간 일러스트 느낌도 나게 만들어줘" | (현재 규칙 적용 시) 겹침 처리 규칙에 따라 더 나중에 등장한 신호만 단일 채택되어 스타일 태그 하나만 반영됨 — 두 스타일을 섞은 혼합 태그는 생성되지 않음 | schemas/AI사진생성.md 겹침 처리 규칙(35~37행) — 축1 정의에 단일/중복 허용 여부에 대한 별도 표시 없음(8~12행) | [?] 사용자가 두 스타일을 동시에 원한다는 의도가 문장에 명확히 드러나는 경우에도 "나중 신호 우선 단일 채택" 방식이 맞는지 — 스키마에 규정 없음, 새로 판단하지 않고 열어둠 |
| T-사진8 — 조립된 태그 순서 고정 여부 | (임의 입력, 예: T-사진1~6의 모든 예시) → 조립 결과의 tag1, tag2, tag3 순서 확인 | 스타일→피사체→구도 순서가 항상 고정인지 확인 필요. (참고: 이 카테고리는 태그나열형 출력이라 다른 카테고리의 "역할지정형 문장" 검증은 필요 없음 — 대신 태그 나열 순서 자체가 검증 대상) | schemas/AI사진생성.md 출력 형식 정의(3행, "tag1, tag2, ..."만 표기·순서 미명시) · F-사진1 결과 설명(04-features.md 12행, 서술 순서만 스타일/피사체/구도) | [?] tag1=스타일·tag2=피사체·tag3=구도 순서가 모든 경우에 고정 계약인지, 아니면 신호 감지 순서 등 다른 기준으로 달라질 수 있는지 — 문서상 명시적 규정 없음 |

## 검토했으나 제외

| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
|---|---|---|
| 카테고리 무관 공통 엣지케이스(빈 입력, 500자 초과, 욕설 등) | 작업 지시(배경 문단) | `docs/docs-공통/06-test-matrix.md`에서 별도로 다루기로 팀이 이미 정함 — 이 문서 범위 아님 |
| 축1·축2·축3 키워드 간 교차 중복(예: 한 키워드가 두 축에 동시에 걸리는 경우) | (없음 — 스키마 검토 중 발견) | schemas/AI사진생성.md의 축1·2·3 키워드 목록을 대조한 결과 서로 겹치는 키워드가 없어 실제로 발생하지 않는 케이스로 판단, 별도 행으로 올리지 않음 |
