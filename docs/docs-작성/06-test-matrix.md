> 문서: 06-test-matrix.md · 상태: 초안 · 열린 질문 [?] 1개 · 채택한 [제안] 0개 · 마지막 갱신: 월
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
>
> 이 문서는 `draft/schemas/작성.md`(전용 축 1~3: 목적·대상·문서유형, 고정 규칙 2개, 겹침 처리 규칙)와 `docs/docs-작성/03~05`, `docs/docs-작성/예시-라이브러리.md`를 직접 읽고 거기서 도출되는 **"작성" 카테고리 고유** 엣지케이스만 다룬다. 카테고리 무관 공통 엣지케이스(빈 입력, 500자 초과, 욕설 등)는 `docs/docs-공통/06-test-matrix.md`에서 별도로 다루므로 여기서는 다루지 않는다. 케이스 ID는 T-작1부터 새로 매겼고, 근거 열의 R·F·P는 `docs/docs-작성/03~05.md`의 최종 ID를 그대로 가리킨다.
>
> [QA 갱신 2026-09-14] T-작2·T-작5의 기대값이 04-features.md·05-policy.md 최신 정책과 어긋나 있음을 QA 검증에서 발견. `AlJalTtack` 리포 `src/categories/writing.ts` 코드는 이미 최신 정책대로 동작 중이며(QA 확인 완료), 낡은 쪽은 이 문서의 기대값이었다 — 코드는 건드리지 않고 아래 두 행만 취소선+대체로 갱신함.
> [QA 갱신 2026-09-14, 2차] T-작9도 같은 이유(F-작1에서 축1이 중복선택으로 이미 확정)로 낡은 기대값임을 확인. `writing.ts`의 `detectPurpose`/`dualToOption`에 T-작9 입력을 실제로 대입 — 좌 신호 "보고"(어절 "보고드리며"가 startsWith로 매칭)·우 신호 "요청"(어절 "요청드립니다."가 startsWith로 매칭)이 모두 감지되어 `PURPOSE_OPTIONS[2]`("정보 전달 + 요청 모두")가 반환됨을 코드로 확인(QA 확인 완료). 코드는 건드리지 않고 아래 T-작9 행만 취소선+대체로 갱신함.
> [QA 갱신 2026-09-14, 3차] T-작1(축4 존재 여부)·T-작7·T-작8(활용형 매칭 방식)도 낡은 `[?]`였음을 확인. T-작1: `draft/schemas/README.md`는 이미 "작성" 전용 축 개수를 3으로 수정했고, `docs/docs-작성/03-requirements.md`·`04-features.md`의 "[동기화 2026-09-14]" 메모도 "축 4개"→"축 3개", "F-작4 제거"로 이미 정리돼 있음(축4/긴급도는 스키마 갱신 중 삭제된 것으로 확정). T-작7·T-작8: `draft/schemas/작성.md` 축1(16행)·축2(23행)·축3(30행)에 "매칭 참고" 절이 이미 명시돼 있고(명사 어근=startsWith, 구문형=그대로 매칭), `04-features.md`의 "[추가 정정 2026-09-14]" 메모도 이를 반영. `src/categories/writing.ts`의 `hasNounRoot`(startsWith)·`hasLiteral`(그대로 매칭) 함수가 이 구분대로 구현돼 있고, `TARGET_RIGHT_NOUN`에는 이미 "형"이 없음(04-features.md "[동기화 2026-09-14]" 메모: "축2 우 신호에서 '형' 제거")을 코드로 확인(QA 확인 완료). 코드는 건드리지 않고 아래 T-작1·T-작7·T-작8 행과 「검토했으나 제외」 표만 취소선+대체로 갱신함.

## 테스트 케이스
| 케이스 | 예시 입력 | 기대 동작 | 근거(스키마/R/F/P) | 확인 필요 |
|---|---|---|---|---|
| T-작1 | (해당 없음 — 스키마 정의 자체의 불일치) | ~~`draft/schemas/README.md`의 카테고리 목록 표는 "작성"의 전용 축 개수를 **4개**로 표기하고, `docs/docs-작성/03·04.md`(R-작4·F-작4)도 "작성.md 축4(긴급도)"를 근거로 좌 신호(일반적으로, 보통, 시간 있을 때, 언제든, 오늘/내일)·우 신호(긴급, 급하게, 지금 당장, 2시간 이내, 오늘 중에, 즉시, ASAP, 급함)를 명시한다. 그런데 오늘(2026-09-14) 실제로 읽은 `draft/schemas/작성.md` 파일 본문에는 축1(목적)·축2(대상)·축3(문서유형) 3개만 정의돼 있고 축4(긴급도)가 없다. 스키마 파일이 갱신되며 축4가 빠진 것인지, 03/04 작성 시점에 축4가 다른 출처(예: 초안 단계 자료)에서 온 것인지 확인 필요~~ — 이미 해소됨(QA 2026-09-14, 3차). `draft/schemas/README.md`는 이미 "작성" 전용 축 개수를 **3**으로 수정했고, `docs/docs-작성/03-requirements.md`("[동기화 2026-09-14]" 메모: "축4(긴급도) 삭제 언급 정리, '축 4개'→'축 3개' 정정")·`docs/docs-작성/04-features.md`("[동기화 2026-09-14]" 메모: "축4(긴급도) 삭제에 따른 F-작4 제거")도 모두 "축 3개, 축4 없음, F-작4 삭제됨"으로 정리돼 있다. 축4(긴급도)는 스키마 갱신 과정에서 삭제된 것으로 확정 — 더 이상 확인 필요 없음 | ~~draft/schemas/README.md(카테고리 목록 표) vs draft/schemas/작성.md(전용 축 본문) vs R-작4·F-작4~~ → draft/schemas/README.md(축 3개로 수정됨), 03-requirements.md·04-features.md [동기화 2026-09-14] 메모(위 인용) | 아니오 |
| T-작2 | "이번 분기 현황 공유 겸, 검토 부탁 드립니다." | ~~축1(목적) 좌 신호 "현황"·"공유"가 먼저, 우 신호 "검토 부탁"이 나중에 등장 → 겹침 처리 규칙(더 나중에 등장한 신호 우선)에 따라 우(요청/승인받기)로 확정~~ — 코드/정책과 대조 결과 낡은 기대값이었음(QA 2026-09-14). F-작1(04-features.md)에서 축1(목적)은 이미 중복선택(체크박스) 축으로 확정: "좌(정보 전달/보고)와 우(요청/승인)가 한 문장에서 동시에 참일 수 있어(예: "보고드리며 승인도 요청드립니다") 감지된 신호를 모두 반영한다", 예외열: "**중복선택 축이라 겹침 처리(나중 신호 우선) 대상이 아님** — 좌우 신호가 한 문장에 동시에 있으면 둘 다 반영". 새 기대값: 좌 신호(명사 어근) "현황"·"공유"와 우 신호(구문형) "검토 부탁"이 한 문장에 모두 있으므로 겹침 처리 대상이 아니라 둘 다 반영 → 목적 축 = 좌(정보 전달/보고) + 우(요청/승인) 모두 확정("정보 전달 + 요청 모두") | ~~작성.md 겹침 처리 규칙, F-작1 예외열~~ → F-작1(04-features.md) 결과열·예외열(위 인용) | 아니오 |
| T-작3 | "거래처 담당자와 같은 팀 동료에게 보낼 공지야." | 축2(대상) 좌 신호 "거래처"가 먼저, 우 신호 "같은 팀"·"동료"가 나중에 등장 → 우(동료/팀원)로 확정. 이 경우 P-작2(존댓말 강제)는 적용되지 않음 — 우 확정 시 어투 규정은 T-작11 참고 | 작성.md 겹침 처리 규칙, F-작2, P-작2 | 아니오 |
| T-작4 | "이메일로 보낼 보고서 초안이야." | 축3(문서유형) 좌 신호 "이메일"이 먼저, 우 신호 "보고서"가 나중에 등장 → 우(체계적 형식)로 확정 → P-작1에 따라 항목별 소제목·번호 매기기가 포함된 구조화된 템플릿 형식으로 결과 제공 | 작성.md 겹침 처리 규칙, F-작3, P-작1 | 아니오 |
| T-작5 | "이거 좀 써줘." | 세 축(목적·대상·문서유형) 모두 좌/우 신호 없음 → 각 축 기본값 그대로 적용: 목적=좌(정보 전달/보고하기), 대상=좌(상급자/외부인), 문서유형=좌(간단한 형식). ~~템플릿·존댓말 등 조건부 고정 규칙은 미적용~~ — 코드/정책과 대조 결과 낡은 기대값이었음(QA 2026-09-14). P-작2(05-policy.md): "대상 축 값 = 상급자·외부인(좌)으로 확정된 상태일 때 ... 그 상태에서는 존댓말·공손한 표현 외의 어투로 결과를 제공할 수 없다" — 신호로 명시 매칭됐는지 기본값으로 떨어졌는지는 구분하지 않음. F-작2(04-features.md) 결과열도 동일: "좌(상급자·외부인)로 확정되면 P-작2에 따라 존댓말·공손한 표현이 결과에 반영됨". 새 기대값: 대상 축이 기본값(좌)으로 떨어져도 축 값 자체는 좌이므로 **존댓말 지침(P-작2)은 적용되어야 함**. 템플릿(P-작1)은 문서유형 축이 여전히 기본값(좌: 간단한 형식)이므로 미적용 그대로 유지 | ~~작성.md 각 축 기본값 정의, F-작1~F-작3 예외열~~ → P-작2(05-policy.md), F-작2 결과열(위 인용) | 아니오 |
| T-작6 | "회사에 제출할 자기소개서를 작성해줘. 입사 기회를 얻을 수 있도록 내 경험과 강점을 상세하게 정리해주고 오늘 완성하고 싶어." (예시-라이브러리 #자기소개서 원문) | 목적·대상 축은 신호 없어 기본값(좌, 좌) 유지, 문서유형 축만 "상세하게"(우 신호)로 매칭되어 체계적 형식으로 확정 → P-작1 적용. 라이브러리가 이미 "목적: 기본값 / 대상: 기본값 / 문서 유형: 체계적 형식"으로 명시한 결과와 일치해야 함 | 예시-라이브러리.md #자기소개서, F-작1~F-작3, P-작1 | 아니오 |
| T-작7 | "이번 분기 실적 보고드립니다." / "어제 팀장님께 보고했어요." | ~~축1 좌 신호 "보고"가 활용형("보고드립니다", "보고했어요")에서도 인식되는지는 `draft/schemas/작성.md`에 매칭 방식 자체가 명시돼 있지 않아 불분명하다(의료질문·개인상담 스키마에는 "매칭 참고" 절에 어근+startsWith 원칙이 명시돼 있으나 작성.md에는 그런 절이 없음). 정확 일치(토큰===키워드)로 구현되면 두 예시 모두 매칭 실패, 부분 포함/startsWith로 구현되면 매칭 성공 — 구현 방식에 따라 결과가 갈림~~ — 전제가 낡았음(QA 2026-09-14, 3차). `draft/schemas/작성.md` 축1에 이미 "매칭 참고" 절(16행)이 있음: "명사 어근 키워드(보고·전달·공유·완료·결과·현황·공지·요청·결재)는 순수 명사라 조사가 붙어도(보고서·보고드립니다·전달할게요 등) 각 어절이 이 어근으로 시작하는지(startsWith)만 확인하면 조사·활용형과 무관하게 감지된다". `docs/docs-작성/04-features.md`의 "[추가 정정 2026-09-14]" 메모도 동일 내용 반영. `src/categories/writing.ts`의 `hasNounRoot`(startsWith 기반)가 이 원칙 그대로 구현돼 있음(QA 확인 완료) — 두 예시("보고드립니다", "보고했어요") 모두 "보고" 어근에 startsWith로 매칭 성공하므로 매칭 방식 불명 문제 해소 | ~~작성.md 축1 신호 키워드(매칭 참고 없음) vs 의료질문.md 축2 매칭참고, 개인상담.md 축1 매칭참고~~ → draft/schemas/작성.md 축1 매칭 참고(16행), 04-features.md [추가 정정 2026-09-14] 메모, writing.ts hasNounRoot | 아니오 |
| T-작8 | "이 보고서를 형식적으로 작성해줘." | ~~의도는 축3 우 신호 "형식적" 매칭 → 문서유형=우(체계적 형식). 다만 축2 우 신호 목록의 어근 "형"(형/누나/언니/오빠)이 만약 부분 포함(includes) 방식으로 매칭된다면 "형식적" 문자열 안에 "형"이 포함돼 있어 축2도 우(동료/팀원)로 오탐될 수 있음 — 개인상담.md 매칭참고가 이미 지적한 "형"↔"형식"·"형편" 겹침 우려와 동일한 문제가 작성.md에도 그대로 존재. T-작7과 같은 매칭 방식 미정 문제에서 파생~~ — 이미 해소됨(QA 2026-09-14, 3차). `04-features.md`의 "[동기화 2026-09-14]" 메모("축2 우 신호에서 '형' 제거")에 따라 `draft/schemas/작성.md` 축2 우 신호 목록(21행: 팀원, 동료, 같은 팀, 우리 팀, 후배, 옆 부서, 같은 직급, 누나, 언니, 오빠)과 `writing.ts`의 `TARGET_RIGHT_NOUN`(팀원, 동료, 후배, 누나, 언니, 오빠) 모두 "형"이 이미 없음(QA 확인 완료) — "형식적"이라는 어절이 "형"으로 시작해 축2가 오탐될 가능성 자체가 목록에서 사라짐. 새 기대값: 문서유형 축만 "형식적"이 "형식"(축3 우 신호)에 startsWith로 매칭되어 우(체계적 형식)로 확정, 대상 축(축2)은 매칭 대상 신호가 없어 기본값(좌) 유지 | ~~작성.md 축2·축3 키워드 목록 대조, 개인상담.md 축1 매칭참고~~ → 04-features.md [동기화 2026-09-14] 메모, draft/schemas/작성.md 축2(21행), writing.ts TARGET_RIGHT_NOUN | 아니오 |
| T-작9 | "보고드리며 승인도 요청드립니다." | ~~축1(목적) 좌(보고)·우(승인 요청)가 한 문장 안에서 의미상 동시에 참일 수 있는 경우. 현재 방식(단일 선택, 더 나중에 등장한 신호 우선)대로면 우(요청/승인받기)만 채택되고 "보고" 의도는 사라짐 — 이걸 그대로 단일 선택으로 둘지, 개인상담 축1처럼 중복 선택 방식으로 바꿀지는 팀 내부에서 이미 논의 중이며 조연익님 검토 예정, 아직 확정 아님~~ — 코드/정책과 대조 결과 낡은 기대값이었음(QA 2026-09-14). F-작1(04-features.md)에서 축1(목적)은 이미 중복선택(체크박스) 축으로 확정: "좌(정보 전달/보고)와 우(요청/승인)가 한 문장에서 동시에 참일 수 있어(예: "보고드리며 승인도 요청드립니다") 감지된 신호를 모두 반영한다", 예외열: "**중복선택 축이라 겹침 처리(나중 신호 우선) 대상이 아님** — 좌우 신호가 한 문장에 동시에 있으면 둘 다 반영". 새 기대값: 좌 신호(명사 어근) "보고"(어절 "보고드리며"가 startsWith로 매칭)와 우 신호(명사 어근) "요청"(어절 "요청드립니다."가 startsWith로 매칭)이 한 문장에 모두 있으므로 겹침 처리 대상이 아니라 둘 다 반영 → 목적 축 = 좌(정보 전달/보고) + 우(요청/승인) 모두 확정("정보 전달 + 요청 모두"). 단일/중복 선택 여부의 "조연익님 검토 예정, 아직 확정 아님" 표기도 F-작1로 이미 팀 확정되어 낡음(삭제 대신 위와 같이 취소선 처리) | ~~작성.md 겹침 처리 규칙 vs 개인상담.md 축1(복수 감지 처리)~~ → F-작1(04-features.md) 결과열·예외열(위 인용) | 아니오 |
| T-작10 | "사장님께 보내는 이메일이야." | 축2 좌 신호 "사장님" 매칭 → 대상=좌(상급자/외부인) 확정 → 고정 규칙 조건("상급자, 외부인") 정확히 일치 → P-작2에 따라 존댓말·공손한 표현이 결과에 반영됨 | 작성.md 고정 규칙, F-작2, P-작2 | 아니오 |
| T-작11 | "팀원한테 보낼 메시지야." | 축2 우 신호 "팀원" 매칭 → 대상=우(동료/팀원) 확정 → 고정 규칙 조건 불일치이므로 미적용(정상 동작). 다만 이 경우 결과에 반영되어야 할 구체적 어투(반말 허용 여부, 편한 존댓말 등)는 F-작2·P-작2 어디에도 정의돼 있지 않음 — 04-features.md F-작2에서 이미 지적된 공백 | F-작2([?] 기존 지적), P-작2 | 예 — [?]4 |

## 검토했으나 제외
| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
|---|---|---|
| 축4(긴급도) 신호 키워드 기반 개별 테스트케이스(예: "긴급", "2시간 이내" 등) | F-작4(04-features.md) | 축4가 `draft/schemas/작성.md` 원본 본문에 정의돼 있지 않음(T-작1 참고) — 존재 여부 자체가 불명확한 상태에서 축1~3과 같은 방식으로 테스트케이스를 만들면 지어내는 것이 되므로, ~~T-작1의 확인 결과가 나올 때까지 보류~~ — 이미 해소됨(QA 2026-09-14, 3차). 축4(긴급도)는 스키마 갱신 과정에서 삭제된 것으로 확정(T-작1 참고, 03-requirements.md·04-features.md [동기화 2026-09-14] 메모) — 축4 자체가 존재하지 않으므로 이 후보는 보류가 아니라 최종 제외 |
