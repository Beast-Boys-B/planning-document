> 문서: 06-test-matrix.md (여행계획 카테고리 전용) · 상태: 초안 · 열린 질문 [?] 5개(TC-여행4·6·9·10·11) · 채택한 [제안] 0개 · **[QA 감사 보강] TC-여행1·TC-여행2 근거 서술에 travel.ts 코드 갱신(커밋 2310bea: 축1 "일정" 단독 키워드, 축4 "쉬고 싶" 어근 축약) 반영 — 기대 동작 자체는 변경 없음** · 마지막 갱신: 2026-09-14(월)
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
>
> 이 문서는 `draft/schemas/여행계획.md`(담당 김동윤)·`docs/docs-여행계획/{03-requirements,04-features,05-policy}.md`·`docs/docs-여행계획/예시-라이브러리.md`를 실제로 읽고 도출한 **여행계획 카테고리 고유** 테스트 케이스만 다룬다. 빈 입력·500자 초과·욕설 등 카테고리 무관 공통 엣지케이스는 `docs/docs-공통/06-test-matrix.md`에서 별도로 다루므로 여기서는 다루지 않는다.

## 테스트 케이스
| 케이스 | 예시 입력 | 기대 동작 | 근거(스키마/R/F/P) | 확인 필요 |
|---|---|---|---|---|
| TC-여행1 | "이번 여행 여긴 어때? 그리고 일정도 좀 짜줘" (축1 좌 신호 "여긴 어때" + 축1 우 신호 "일정 짜줘"가 한 문장에 동시 등장) | 축1(목적)은 중복선택 축이므로 좌·우 신호가 둘 다 반영된다 — "목적지 정보 탐색"과 "일정·동선 짜기" 방향이 동시에 토글됨. 겹침 처리 규칙(나중 신호 우선) 적용 대상이 아님 | 여행계획.md 겹침 처리 규칙: "축1(목적)·축4(여행 스타일)은 중복선택 축이라 이 규칙 대상이 아님 — 감지된 좌/우 신호를 모두 반영한다(양쪽 다 토글 가능)." · R-여행1·R-여행2 · F-여행2 · **[보강, QA 감사 — 코드 갱신 커밋 2310bea 반영]** 예시 입력의 "일정도 좀 짜줘"는 원래 우 신호 키워드 "일정 짜줘"와 문자열이 정확히 일치하지 않는다(조사 "도"·부사 "좀"이 끼어듦) — `travel.ts` 축1 우 신호 목록(157행 근처 주석, 162행 배열)에 "일정" 단독 키워드가 추가되어 이런 활용형도 매치된다 | 예시-라이브러리.md "예산 편성" 항목("3박 4일"+"궁금해"가 한 문장에 있는데도 축1 우 태그 없이 "좌"만 표기됨)이 실제로 양쪽 다 반영하는 스펙과 어긋나 보임 — 예시 라이브러리 태그 갱신 필요한지 확인 |
| TC-여행2 | "쉬고 싶은데 그래도 액티비티도 좀 해보고 싶어" (축4 좌 "쉬고 싶어" + 축4 우 "액티비티"가 한 문장에 동시 등장) | 축4(여행 스타일)도 중복선택 축이므로 좌·우 신호가 둘 다 반영된다 — "휴양/힐링"과 "액티비티/체험 중심"이 동시에 토글됨 | 여행계획.md 겹침 처리 규칙(위 인용과 동일 문장, 축4 포함) · R-여행7·R-여행8 · F-여행5 · **[보강, QA 감사 — 코드 갱신 커밋 2310bea 반영]** 예시 입력의 "쉬고 싶은데"는 원래 좌 신호 키워드 "쉬고 싶어"와 활용형이 다르다(어미 "-어"가 아니라 "-은데") — `travel.ts` 축4 좌 신호(211행 근처 주석, 212행 배열)가 "쉬고 싶어"에서 어근 "쉬고 싶"으로 축약되어 "쉬고 싶은데"·"쉬고 싶다" 같은 다른 활용형도 매치된다 | - |
| TC-여행3 | "처음엔 혼자 가려고 했는데 결국 친구랑 같이 가기로 했어" (축2 좌 "혼자"가 먼저, 축2 우 "친구랑"이 나중에 등장) | 축2(동행)는 단일선택 축이므로 나중에 나온 신호("친구랑", 우)가 채택됨 → "동반자 있음"으로 반영 | 여행계획.md 겹침 처리 규칙: "축2(동행)·축3(여행 시점) 등 단일선택 축에서 좌/우 신호가 동시에 감지되면, 나중에 나온 신호를 선택하고 어떤 신호도 없을시 기본값을 따른다" · R-여행3·R-여행4 · F-여행3 · docs-공통/05-policy.md P-공22 | - |
| TC-여행4 | "다음 주에 가는데 아직 어디로 갈지 결정은 못했어" (축3 좌 "다음 주에"가 먼저, 축3 우 "아직... 결정 안 함"이 나중에 등장) | **[해소됨, 김동윤 확인]** 축3은 "나중 신호 우선"의 예외이므로 등장 순서와 무관하게 더 긴급한 상태(좌: 임박한 여행)가 채택된다 → 좌("다음 주에")가 반영되어 "임박한 여행(예약 완료)"으로 정제됨 | 여행계획.md 겹침 처리 규칙 — 축3은 일반 원칙("나중 신호 우선")의 예외이며 긴급 상태(좌)를 우선한다(팀 결정) · R-여행5·R-여행6 · F-여행4 | - |
| TC-여행5 | 매치: "영국으로 5박 6일 여행 가요" / 불매치(대비): "영국으로 5박6일 여행 가요"(박과 숫자 사이 공백 없음) | `[0-9]+박 [0-9]+일` 패턴은 "박"과 뒤 숫자 사이에 공백이 있어야 매치된다 — "5박 6일"은 매치, 공백 없는 "5박6일"은 정규식 리터럴 공백 미충족으로 불매치(→ 축1 우 신호로 인식 안 됨, 다른 우 키워드가 없으면 축1은 기본값(좌)으로 처리될 수 있음) | 여행계획.md 축1 정규표현식 `[0-9]+박 [0-9]+일` · 예시-라이브러리.md "숙소검색" 항목("5박 6일", 공백 있음, 정상 매치 사례) | - |
| TC-여행6 | 매치: "3명이 여행 가요" / "3명이서 여행 가요"(부분일치 여부) / 불매치(대비): "삼명이 여행 가요"(숫자 아닌 한글 수사) | `[0-9]+명(이\|가\|과\|랑\|하고\|하는데\|해서)` 패턴은 "3명이"에서 매치되며, 정규식이 앵커링(전체일치) 없이 부분일치(search)로 적용된다면 "3명이서"도 "3명이" 부분에서 매치될 수 있다. "삼명이"처럼 아라비아 숫자가 아닌 경우는 `[0-9]` 조건 미충족으로 불매치 | 여행계획.md 축2 정규표현식 `[0-9]+명(이\|가\|과\|랑\|하고\|하는데\|해서)`, 우 신호 키워드 목록에 "명이서" 직접 명시됨 · 예시-라이브러리.md "숙소검색" 항목이 "3명이서"를 축2 **좌**로 태그해둠(스키마상 우 신호여야 함 — 표기 오류로 보임) | [?] 정규식이 부분일치(search)로 동작하는지 전체일치(fullmatch)로 동작하는지 구현 방식 확인 필요, 예시 라이브러리 "3명이서→좌" 태그 오표기 여부 김동윤 확인 필요 |
| TC-여행7 | 매치: "3일 후에 출발해요" / 불매치(대비): "3일후에 출발해요"(공백 없음), "2주일 뒤예요"(단위가 "주"가 아니라 "주일") | `[0-9]+(일\|주\|개월) (후\|뒤\|남았는데\|예정)` 패턴은 단위와 "후/뒤" 사이에 공백이 있어야 매치된다 — 공백 없는 "3일후"는 불매치. "주일"은 패턴의 "주" 뒤에 공백이 와야 하는데 "주일"은 공백 없이 "일"이 바로 붙어 불매치 가능성(패턴이 "주" 다음 공백을 요구하므로 "주일 뒤"에서 "주" 뒤 문자가 공백이 아닌 "일"이라 매치 실패) | 여행계획.md 축3 정규표현식 `[0-9]+(일\|주\|개월) (후\|뒤\|남았는데\|예정)` | - |
| TC-여행8 | "그냥 어디 여행 가고 싶은데 아직 목적지는 안 정했어" (자연어 문장에 목적지명을 적지 않은 채 제출) | 목적지는 자유 슬롯이라 필수 입력이 아니다 — 문장에 목적지를 적지 않고 제출해도 목적지 없이 진행되며, F-공2(자연어 입력) 자체의 빈 입력 판정과는 무관하다. placeholder 문구는 목적지 포함 작성을 유도만 할 뿐 강제하지 않는다 | R-여행9 · F-여행1 예외 열: "목적지를 문장에 적지 않고 제출해도 목적지 없이 진행되며(필수 아님)..." | **[해소됨]** 04-features.md·03-requirements.md가 한때 전용 팝업 필드로 갱신됐던 것을, 김동윤(담당자) 확인을 거쳐 스키마 원본의 자유 슬롯(placeholder 유도) 방식으로 최종 확정 — 04/03 두 문서를 스키마와 일치하도록 되돌림 |
| TC-여행9 | "여긴 어때? 그런데 사실 일정 짜는 게 더 급해" 같이 두 신호 중 어느 쪽이 "더 구체적으로 명시"됐는지 모호한 문장 | 겹침 처리 규칙 중 "두 신호가 모호하게 언급된 경우 더 구체적으로 명시된 신호를 선택한다"는 조항이 실제로 어떻게 판정 가능한 정의인지 미정 — 이 조항 자체가 재정의 대기 중 | 여행계획.md 겹침 처리 규칙 줄 50 | [?] "구체적으로 명시된 신호 우선" 규칙은 김동윤이 별도로 재정의 예정(이번 문서에서 새로 판단하지 않음) |
| TC-여행10 | "여행 가고 싶다" (축1·축2·축3·축4 어느 축의 좌/우 신호 키워드·정규식에도 걸리지 않는 완전 중립 문장) | 4개 축 전부 신호가 없으므로 각 축의 기본값이 그대로 적용된다 — 축1 좌(여행지 정보 알아보기) · 축2 좌(혼자) · 축3 우(고려 중) · 축4 좌(휴양/힐링) | 여행계획.md 각 축 "기본값" 줄(축1 줄12 좌·축2 줄18 좌·축3 줄24 우·축4 줄30 좌) · F-여행2~5 예외 열(신호 없으면 기본값 적용) | [?] 이 네 가지 기본값 조합("혼자서 정보 탐색하며 고려 중, 휴양 스타일")이 완전 중립 문장의 결과로 자연스러운지는 스키마에 근거가 없음 — 팀 확인 필요 |
| TC-여행11 | 예시-라이브러리.md "환율" 항목: "곧 미국으로 여행을 떠나는데 현재 환율이 궁금해..." → 축1 좌 태그 사유가 "알려줘"로 표기됨 | "알려줘"는 축1 좌 신호 키워드 목록(어디가 좋을까·뭐가 유명한지·맛집 추천·가볼만 한지·여긴 어때·정보·유명·궁금)에 없는 단어다 — 실제로 매치되는 키워드는 "궁금"이다. 태그 사유 표기가 스키마 키워드와 불일치 | 여행계획.md 축1 좌 신호 키워드 목록 · 예시-라이브러리.md "환율" 항목 | [?] 예시 라이브러리의 태그 사유 표기 오류인지, 아니면 "알려줘"도 좌 신호 키워드로 추가할 의도였는지 김동윤 확인 필요 |

## 검토했으나 제외
| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
|---|---|---|
| 빈 입력·500자 초과·욕설/블랙리스트 키워드 등 카테고리 무관 공통 엣지케이스 | 작업 지시(카테고리 무관 공통 엣지케이스는 docs/docs-공통/06-test-matrix.md에서 다룸) | 이 문서는 여행계획 카테고리 고유(축·키워드·정규식에서 나오는) 엣지케이스만 다루기로 범위가 지정됨 |
| "구체적으로 명시된 신호 우선" 규칙의 구체적 판정 기준을 새로 정의하는 것 | 작업 지시 5번(김동윤이 별도로 재정의 예정) | 팀 대신 정하지 않는다 — TC-여행9로 `[?]` 남김 |
