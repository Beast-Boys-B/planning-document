> 문서: 05-policy.md (여행계획 카테고리 전용) · 상태: 초안 · 열린 질문 [?] 0개 · 채택한 [제안] 0개 · 마지막 갱신: 2026-09-09(수)
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
>
> 이 문서는 팀원 인터뷰가 아니라 `draft/schemas/여행계획.md`를 기획 문서 양식으로 옮긴 것이다. 이 카테고리는 `고정_규칙: []`로, hibro(개인상담) F1의 "안전 문구 고정 삽입" 같은 카테고리 전용 고정 규칙이 없다. 대신 있는 것은 축1~축4 공통으로 적용되는 겹침 처리 규칙뿐이다.

## 정책
| P | 대상 F | 누가 | 언제(상태값) | 규칙 (할 수 없다 · 까지) | 위반 시 앱이 보이는 것 |
|---|---|---|---|---|---|
| (해당 없음) | F-여행1~F-여행5 전체 | — | — | **이 카테고리는 고정 규칙 없음** (`고정_규칙: []`, 여행계획.md) — hibro P1(안전 문구 고정 삽입)처럼 항상 삽입되는 문구나 축 간 충돌 처리 규칙(hibro P3·P4 같은 것)이 이 카테고리엔 없다 | — |

## 상태값
축1(목적)·축2(동행)·축3(여행 시점)·축4(여행 스타일)은 저장되는 상태값이 아니라, 매 자연어 입력마다 문장에서 새로 추출되는 값이다(hibro 개인상담 F1과 같은 성격). 카테고리 선택 → 자연어 입력 → 신호 인식/정제 → 재조정이라는 세션 전체의 상태값 전이는 이 카테고리에 별도로 있는 것이 아니라 `docs/docs-공통/05-policy.md`의 통용 상태값(woomin·yeonsak 흐름)을 그대로 따른다.

## 서비스 정책
| SP | 한 줄 |
|---|---|
| (이 카테고리 로컬 SP 없음) | 겹침 처리 규칙은 **공통 정책으로 승격됨** — `docs/docs-공통/05-policy.md` 의 **P-공22** 참고. 이 문서에 있던 SP-여행1(겹침 처리, 로컬)은 삭제하고 공통 참조로 대체함 |

## 검토했으나 제외
| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
|---|---|---|
| (없음) | | 이번 문서는 인터뷰·발산이 아니라 확정된 스키마(여행계획.md) 번역 작업이라 검토 후 제외된 후보 자체가 없음 |

## 겹침 처리 규칙 — 공통 승격 확정 (갱신)
5개 카테고리(개인상담·의료질문·여행계획·AI사진생성·작성) 스키마 전부에 동일한 겹침 처리 규칙 문구가 있다는 판단 근거가 모두 모여, `docs/docs-공통/05-policy.md`의 **P-공22**로 승격 확정됐다. 이전에 이 문서가 "hibro·다른 카테고리와 형식이 어긋난다"는 이유로 로컬(SP-여행1)로 남겨뒀던 판단은 이번 팀 결정으로 해소됐다 — 위 서비스 정책 표를 공통 참조로 갱신했다.
