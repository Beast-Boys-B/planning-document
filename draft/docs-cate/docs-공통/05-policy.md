> 문서: 05-policy.md (공통/통용 규칙 — 카테고리 무관) · 상태: 초안(1단계 통합, 병합 안 함) · 열린 질문 [?] 다수(출처별 원본 유지) · 채택한 [제안] 0개 · 충돌 3건 전부 해소(로그인 여부·재조정 루프 상한·글자 수 상한) + 관련 v2 결정 1건(단축키 호출) 반영 · 신규 승격 1건(P-공22, 겹침 처리 규칙) · 마지막 갱신: 화
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.
>
> woomin·yeonsak 의 05-policy.md 중 통용(카테고리 무관) 정책만 모았다. dongyoon 은 05 파일 자체가 없어 대상 없음. **서로 다른 값으로 충돌하는 항목은 절대 하나로 합치지 않고 둘 다 남긴 뒤 "충돌" 표시를 했다** — 팀이 정할 때까지 그대로 둔다.

## 정책
| P | 대상 F(원본 기준) | 누가 | 언제(상태값) | 규칙 (할 수 없다 · 까지) | 위반 시 앱이 보이는 것 | 출처 | 비고(중복·충돌) |
|---|---|---|---|---|---|---|---|
| P-공1 | F-공1(woomin F1) | 모든 방문자(제약 없음) | 무관함(로그인 전/후·점검 등과 무관하게 항상) | 카테고리를 선택하지 않으면 다음 단계로 진행할 수 없다 | 다음 단계로 넘어가지지 않는다 | woomin P1 | P-공9(yeonsak P1)와 같은 개념 |
| P-공2 | F-공2(woomin F2) | A1(사용자) | 카테고리 선택 후 자연어 입력 화면에서 [프롬프트 만들기]를 누르는 시점 | 입력이 비어 있으면 프롬프트 생성을 실행할 수 없다 | [프롬프트 만들기] 버튼이 눌리지 않는다 | woomin P2 | P-공10(yeonsak P2)와 같은 개념 |
| P-공3 | F-공3(woomin F3) | A1(사용자) | F2 실행 직후, 신호 추출·정제 프롬프트 생성이 실행되는 시점 | 신호가 하나도 감지되지 않아도 에러로 처리할 수 없다(반드시 기본값으로 채워야 한다) | 기본값으로 자동 채워진 정제 프롬프트가 정상적으로 표시된다(에러 화면 없음) | woomin P3 | 관련: P-공11(yeonsak P3)은 "재분석 반복" 쪽 이슈라 개념이 약간 다름 |
| P-공4 | F-공4(woomin F4) | A1(사용자) | 정제 프롬프트 생성이 끝나고 원본·정제본 비교 화면에 진입하는 시점 | 정제 결과가 원본과 완전히 같아도 비교 화면 표시를 생략할 수 없다 | 원본과 정제본이 동일하다는 안내 문구가 표시된다 | woomin P4 | 대응 항목 없음(yeonsak엔 비교 화면 자체가 없음 — 04 F-공4 비고 참고) |
| P-공5 | F-공5(woomin F5) | A1(사용자) | 비교/설정 확인 화면에 머무는 전체 구간 | 톤·길이·축 토글 조정은 제한 없이 언제든 가능하다 | 해당 없음 | woomin P5 | P-공12(yeonsak P3의 옵션수정 제한없음 부분)와 개념 유사 |
| P-공6 | F-공6(woomin F6) | A1(사용자) | S4→S5 전환 지점 | 재조정 시 자연어를 다시 입력하지 않고는 못 돌아가게 막을 수 없다 | 해당 없음 | woomin P6 | 관련: P-공12(재조정 횟수 관련 소프트 넛지)와 같은 재조정 루프 영역 — 이 항목 자체는 충돌 없음 |
| P-공7 | F-공1(woomin F1) | A1(사용자) | 카테고리 선택 화면에서 사용자가 카테고리 하나를 누르는 시점 | 카테고리는 한 번에 하나만 선택할 수 있다(복수 선택 불가) | 기존 선택 해제 후 새 카테고리로 교체 | woomin P7 | 대응 항목 없음(yeonsak엔 이 명시적 제약 없음) |
| P-공8 | F-공2(woomin F2) | A1(사용자) | 자연어 입력창에 텍스트를 입력하는 시점(글자 수 상한을 넘으려는 순간) | 자연어 입력은 최대 500자까지만 허용한다 | 500자를 넘으면 초과 안내 문구가 뜨고 버튼이 비활성화된다 | woomin P8 | **팀 결정(해소됨)**: 500자로 최종 확정. P-공17(yeonsak P2, 과거 "미확정"이었음)도 이 결정으로 확정됨 — 더 이상 충돌 아님 |
| P-공9(P9-A) | F-공2(woomin F2) | A1(사용자) | [프롬프트 만들기]를 눌러 제출하는 시점 | 욕설 키워드 블랙리스트에 걸리는 입력은 그대로 처리할 수 없다 | 블랙리스트 키워드 부분에 경고·마스킹 처리 | woomin P9-A | 대응 항목 없음(yeonsak 문서엔 욕설 필터 언급 없음) |
| P-공10 | F-공3(woomin F3) | A1(사용자) | F2 제출 직후, 신호 추출 로직 실행 시점 | 여러 축 신호가 충돌하면 임의 적용 불가(정해진 우선순위대로 적용) | 우선순위 높은 축 값 적용, 나머지 기본값 | woomin P10 | 관련: P-공22("겹침 처리 규칙", 같은 축 내 다중신호)와는 다른 개념(이건 축간 충돌) — 둘 다 F-공3 시점에 함께 적용됨 |
| P-공11 | F-공5(woomin F5) | A1(사용자) | 설정 확인 화면에서 토글 조정 시점(동시 변경 포함) | 여러 토글 동시 조정 시 값 불일치 불가 | 최신 값 기준 즉시 재계산 | woomin P11 | 대응 항목 없음(yeonsak엔 이 명시적 규칙 없음) |
| P-공12 | F-공6(woomin F6) | A1(사용자) | S3~S4 판단 후 S5(재조정)로 돌아가는 매 회차 시점 | 3회 이상 조정 시 도움말(소프트 넛지) 표시(강제 종료·상한 아님) | 도움말 팝업 표시(제한 아님, 계속 조정 가능) | woomin P12 | **팀 결정(해소됨)**: 이 내용("3회 이상 시 소프트 넛지")으로 최종 확정. P-공15(yeonsak P7, 과거 "횟수 제한 없음"이었음)는 이 결정으로 대체됨 |
| P-공13 | F-공7(woomin F7) | A1(사용자) | 결과 확인 화면에서 [복사해서 사용하기]를 누르는 시점 | 클립보드 복사 실패 상태로 "복사됨" 표시 불가 | 복사 실패 안내 + 수동 복사 영역 표시 | woomin P13 | 관련: yeonsak F-공13 예외에 "클립보드 권한 차단"이라는 새 케이스가 있음(04 참고) — woomin P13은 "실패" 일반, yeonsak은 "권한 차단"이라는 특정 원인 — 서로 보완 관계일 수 있음 |
| P-공14 (P1) | F-공10(yeonsak F1) | A1·A2 모두 동일, 로그인·권한 제한 없음 | 별도 상태 제한 없음, 카테고리 화면 진입하면 사용 가능 | 선택·재선택 횟수·시간 제한 없음(정책 없음, 팀 확인) | 이용 제한 없음. 카테고리 미선택 시 안내 문구 | yeonsak P1 | P-공1·P-공7(woomin P1·P7)과 개념 겹침, "선택 개수 제한"은 yeonsak엔 명시 안 됨 |
| P-공15 (P7) | F-공16(yeonsak F7) | A1·A2 모두 동일 | S4에서 프롬프트가 정상 생성되어 결과가 표시된 상태 | ~~수정·재생성 횟수 제한 없음~~ → **팀 결정으로 대체됨**: P-공12(woomin P12, "3회 이상 조정 시 도움말/소프트 넛지 표시")로 확정 | 제한 없어 위반 처리 없음. S4 결과 없으면 '옵션 수정하기' 사용 불가 (단, 3회 이상부터는 P-공12 대로 소프트 넛지 추가) | yeonsak P7 | **팀 결정(해소됨)**: 원래 표기("횟수 제한 없음")는 P-공12 결정으로 대체됨. 원문 그대로 취소선으로 남기고 대체 내용을 병기함 |
| P-공16 (P8) | F-공1~F-공16 전체(세션 상태 전반) | A1·A2 (동일 사용자가 여러 탭·창에서 동시 사용) | 동일 사용자가 여러 탭·창에서 각각 다른 카테고리·입력으로 세션을 동시 진행할 때 | 각 탭·창은 서로 완전히 독립된 세션 상태를 가진다(탭 간 상태 공유 없음) — 한 탭에서 다른 카테고리·입력을 진행해도 다른 탭에 영향을 주지 않는다 | 해당 없음(충돌 자체가 구조적으로 발생하지 않음) | yeonsak P8 | **팀 결정(확정)**: 독립 세션으로 확정 — SP-공1(로그인 없이 세션 기반 유지)과 일관된 방향. 탭별 독립 세션이라 애초에 "어느 탭이 이기는지" 같은 충돌 자체가 발생하지 않음 |
| P-공17 (P2) | F-공11(yeonsak F2) | A1·A2 모두 동일, 로그인·권한 제한 없음 | F1에서 카테고리를 선택한 상태에서 사용 가능 | 입력 횟수 제한 없음. 글자 수 상한은 500자로 생각 중(팀 결정으로 확정) | 미입력 상태로 '분석' 누르면 안내, 진행 불가 | yeonsak P2 | **팀 결정(해소됨)**: P-공8(woomin P8)과 함께 500자로 최종 확정 — 더 이상 충돌 아님 |
| P-공18 (P3) | F-공12(yeonsak F3) | A1·A2 모두 동일 | F2에서 자연어 입력 후 '분석' 완료된 상태 | 옵션 추가·수정 개수·재분석 횟수 제한 없음 | 자동 분석 실패 시 안내 문구 | yeonsak P3 | 관련: P-공10(woomin P10, 축 충돌 우선순위)·P-공11(woomin P11, 토글 동시조정)과 개념 인접. **신규 [?]**: "재분석 요청을 연속으로 여러 번 보낼 때 처리 순서"는 woomin 문서엔 없던 논점 |
| P-공19 (P4) | F-공13(yeonsak F4) | A1·A2 모두 동일 | F3에서 프롬프트가 정상 생성된 상태 | 재생성·복사 횟수 제한 없음 | 생성 실패·복사 실패 시 각각 안내 | yeonsak P4 | P-공13(woomin P13, 복사 실패 처리)과 개념 유사 |
| P-공20 (P5) | F-공14(yeonsak F5) | A1·A2 모두 동일 | 작업 중인 화면과 관계없이 호출 가능, 창 중복 방지 | 호출 횟수 제한 없음. 단축키 키조합은 [?] | 단축키 충돌 등으로 호출 불가 시 안내 표시 | yeonsak P5 | **팀 결정 연동**: 이 정책이 딸린 F-공14 자체가 v2(차후구현)로 확정됨(위 04-features.md 참고) — 이 정책도 MVP 대상 아님 |
| P-공21 (P6) | F-공15(yeonsak F6) | A1·A2 모두 동일 | 자연어 분석이 완료되어 옵션 확인 화면에 있는 상태 | 카테고리 재선택 횟수 제한 없음 | 미선택 시 변경 미적용, 취소 시 기존 유지 | yeonsak P6 | 대응 항목 없음(woomin엔 "카테고리 재선택" 재진입 지점 자체가 없음 — 04 F-공15 비고 참고) |
| **P-공22** | F-공3(신호 추출 실행 시점, 모든 카테고리 공통) | A1(사용자) | 신호 추출(자동 정제)이 실행되는 시점 | 같은 축 내에서 좌/우(또는 다중 선택지) 신호가 문장 안에 동시에 감지되면, 임의로 아무거나 채택할 수 없다 — 문장에서 더 나중에 등장한 신호를 우선 채택해야 한다("겹침 처리 규칙") | 해당 없음(엔진 내부 동작 규칙이라 사용자에게 위반 화면이 뜨지 않음) — 우선순위가 낮은(먼저 등장한) 신호는 조용히 무시된다 | hibro SP1(개인상담) / draft/schemas/ 5개 카테고리(개인상담·의료질문·여행계획·AI사진생성·작성) 전부 동일 문구 확인 | **공통 정책으로 승격 확정**(이번 결정). 기존 P-공10(축 간 충돌, 서로 다른 축끼리)과는 다른 개념 — P-공22는 "같은 축 안"의 다중 신호 문제. 각 카테고리 로컬 문서(의료질문 SP1, 여행계획 SP-여행1, hibro SP1 등)는 이 정책을 참조하는 형태로 갱신 |

## 상태값 (출처별, 병합 안 함)
- **woomin**: 카테고리 선택 전 → 카테고리 선택됨 → 프롬프트 정제 완료 → 재조정 중 → (3회 이상 조정 시) 도움말(소프트 넛지) 안내 · 전이 조건: 각 단계는 이전 단계 결과가 확정돼야 다음으로 넘어가며, 재조정 중 3회 이상 조정하면 "도움말 안내" 상태로 전환되지만 이후에도 계속 조정 가능(강제 종료·상한 없음)
- **yeonsak**: 카테고리 선택 중(F1) → 자연어 입력 중(F2) → 분석 중(F2→F3 전이) → 옵션 확인·수정 중(F3) → 프롬프트 생성 중(F3→F4 전이) → 생성 완료(F4) · F6(카테고리 재선택)은 옵션 확인·수정 중 → 카테고리 선택 중으로 되돌아가는 전이 · F7(옵션 수정하기)은 생성 완료 → 옵션 확인·수정 중으로 되돌아가는 전이

> [관찰] 두 상태값 흐름은 큰 그림은 비슷하지만(선택→입력→정제/분석→조정→완료), woomin은 "3회 이상 조정 시 넛지"라는 상태를 명시하고 yeonsak은 명시하지 않음 — 위 P-공12/P-공15 충돌과 같은 뿌리의 차이로 보인다.

## 서비스 정책
| SP | 한 줄 | 출처 | 비고 |
|---|---|---|---|
| SP-공1 | MVP는 로그인 없이 세션 기반으로만 정제 결과가 유지된다(기록 저장·불러오기 기능(F9)은 이 결정과 일관되게 v2로 확정). 단, **로그인 기능 자체는 v2 이후 도입 가능성을 열어둔다**(영구 확정 아님 — 팀 결정, 갱신) | woomin SP1 | **팀 결정(갱신)**: "MVP=로그인 없음, 로그인 기능은 v2 이후 도입 가능성 열어둠"으로 수정. 연계 기능인 F9(기록 보기)의 v2 확정과 일관됨 |
| SP-공2 | ~~로그인 필요 여부 등은 팀이 아직 고려하지 않기로 함~~ → **팀 결정으로 해소됨**: 로그인 필요 여부는 SP-공1(로그인 없음)로 확정. 단, 데이터 보관 기간·이용 연령 제한·유료/무료 여부는 이번 결정 범위 밖이라 여전히 `[?]` | yeonsak SP1 | **해소됨**: 로그인 여부에 대한 SP-공1/SP-공2 충돌은 "로그인 없음"으로 정리됨. 나머지 미정 사항(보관기간·연령·유료화)은 별도 미결로 남음 |

> [관찰 → 확정 반영됨] hibro(개인상담)의 SP1("같은 축 안에서 신호가 여러 개 동시에 감지되면 더 나중에 등장한 신호를 우선 채택")이 draft/schemas/ 의 5개 카테고리 스키마 전부에 동일한 문구("겹침 처리 규칙")로 반복된다는 관찰이, 5개 카테고리 문서가 모두 채워지며 판단 근거가 모여 **공통 정책 P-공22로 승격 확정**됐다. hibro 원본(`draft/docs-hibro/05-policy.md`)은 이번에도 건드리지 않았고, 대신 `draft/docs-개인상담/05-policy.md`(카테고리 이름 폴더로 재구성)에서 이 승격을 반영했다.

## 보류(v2)
| 후보 | 출처(제안 근거) | 보류 사유 |
|---|---|---|
| P9-B (개인정보 감지): 개인상담·의료 등 민감한 내용(전화번호·주민번호 등)이 포함된 입력에 대한 처리 | woomin 05 보류표 — [제안] 근거: A1 한 줄 상황 "개인상담이나 의료분야 질문" | 지명 같은 자유 슬롯 문제와 똑같이 규칙 기반으로 못 잡는 영역이라 v2로 미룸(팀 결정) |

## 검토했으나 제외
| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
|---|---|---|
| P12(원안) 재조정 루프(F5↔F6) 하드 리밋 — 최대 반복 횟수 이후 강제로 막고 대안 안내 화면을 띄움 | woomin 05 제외표 — [제안] 근거: S5·S6 불편 "감으로 시행착오를 반복"·"포기" | 재조정 루프를 줄이는 게 핵심 가치라 하드 리밋을 걸면 설계 의도와 충돌함(팀 결정) |
| 비로그인·무료 사용자에 대한 일 사용량 제한(예: 하루 N회) | yeonsak 05 제외표 — 코치 [제안] 근거: 일반적 SaaS 무료 티어 관행 | 팀 거절 |
| 서비스 이용 연령 제한 여부 | yeonsak 05 제외표 — 코치 [제안] 근거: 01-problem.md 대상 절에 연령 하한선 미언급 | 팀 거절 |

## 해소된 충돌 (팀 결정 반영, 더 이상 열려있지 않음)
1. **로그인 여부** → "MVP는 로그인 없음, 로그인 기능은 v2 이후 도입 가능성 열어둠"으로 확정(SP-공1, 갱신됨 — 이전엔 "로그인 없음"으로 영구 확정이었으나 팀이 v2 이후 여지를 다시 열었음). yeonsak SP1(SP-공2)의 "미정" 표기는 최초 결정으로 해소된 상태 그대로. 단, SP-공2 안의 데이터 보관 기간·이용 연령 제한·유료/무료 여부는 이번 결정 범위 밖이라 여전히 미결.
2. **재조정 루프 상한** → "3회 이상 조정 시 소프트 넛지(도움말) 표시"로 확정(P-공12). yeonsak P7(P-공15)의 "횟수 제한 없음" 표기는 이 결정으로 대체됨.
3. **자연어 글자 수 상한** → "500자"로 확정(P-공8). yeonsak P2(P-공17)의 "미확정" 표기는 이 결정으로 해소됨.
4. **"단축키로 어디서든 호출" 요구의 MVP/v2 여부** → 요구 자체는 유효하나 MVP 대상에서 빼고 **v2(차후구현)**로 확정(03-requirements.md R-공13, 04-features.md F-공14·05-policy.md P-공20 모두 동일하게 반영).
