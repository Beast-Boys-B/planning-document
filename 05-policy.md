> 문서: 05-policy.md · 상태: 초안 · 열린 질문 [?] 6개 · 채택한 [제안] 4개 · 마지막 갱신: 화요일
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. [?] 는 팀이 아직 모르는 것이다.

## 정책
| P | 대상 F | 누가 | 언제(상태값) | 규칙 (할 수 없다 · 까지) | 위반 시 앱이 보이는 것 |
|---|---|---|---|---|---|
| P1 | F1 | A1·A2 모두 동일, 로그인·권한 제한 없음 | 별도 상태 제한 없음, S1 화면에 진입하면 사용 가능 | 선택·재선택 횟수·시간 제한 없음 (정책 없음, 팀 확인) | 이용 제한 없어 위반 처리 없음. 단, 카테고리 미선택 시 '카테고리를 선택해주세요' 안내 |
| P2 | F2 | A1·A2 모두 동일, 로그인·권한 제한 없음 | F1에서 카테고리를 선택한 상태에서 사용 가능 | 입력 횟수 제한 없음. 글자 수 상한은 500자로 생각 중(미확정, 팀 표현 그대로) | 글자 수 상한이 미확정이라 초과 시 처리 방식도 [?]. 단, 미입력 상태로 '분석' 누르면 '원하는 내용을 입력해주세요' 안내, 진행 불가 |
| P3 | F3 | A1·A2 모두 동일, 로그인·권한 제한 없음 | F2에서 자연어 입력 후 '분석' 버튼으로 분석 완료된 상태에서 사용 가능 | 옵션 추가·수정 개수, 재분석 횟수 제한 없음. (발산 채택, 구체적 정책 미정) 재분석 요청을 연속으로 여러 번 보낼 때 처리 순서는 [?] | 제한 없어 위반 처리 없음. 단, 자동 분석 실패 시 '자동으로 선택된 옵션이 없습니다. 필요한 옵션을 직접 선택해주세요' 안내 |
| P4 | F4 | A1·A2 모두 동일, 로그인·권한 제한 없음 | F3에서 옵션 확인·수정 후 '프롬프트 생성' 버튼으로 프롬프트가 정상 생성된 상태에서 사용 가능 | 재생성·복사 횟수 제한 없음 | 제한 없어 위반 처리 없음. 다만 생성 실패 시 '프롬프트를 생성하지 못했습니다. 다시 시도해주세요', 복사 실패 시 '복사하지 못했습니다. 다시 시도해주세요' 안내 |
| P5 | F5 | A1·A2 모두 동일, 로그인·권한 제한 없음 | 작업 중인 화면과 관계없이 호출 가능. 이미 창이 열려있으면 새 창 대신 기존 창을 앞으로 표시 | 호출 횟수 제한 없음. (발산 채택, 구체적 정책 미정) 호출 단축키의 구체적인 키 조합은 [?] | 제한 없어 위반 처리 없음. 단축키 충돌 등으로 호출 불가 시 호출되지 않으며 단축키 설정 변경 등 안내 표시 |
| P6 | F6 | A1·A2 모두 동일, 로그인·권한 제한 없음 | 자연어 분석이 완료되어 S3 화면에 있는 상태에서 사용 가능 | 재선택 횟수 제한 없음 | 제한 없어 위반 처리 없음. 아무 카테고리 미선택 시 변경 미적용, 취소 시 기존 카테고리·입력 유지한 채 S3으로 복귀 |
| P7 | F7 | A1·A2 모두 동일, 로그인·권한 제한 없음 | S4에서 프롬프트가 정상 생성되어 결과가 표시된 상태에서 사용 가능 | 수정·재생성 횟수 제한 없음 | 제한 없어 위반 처리 없음. 다만 S4 결과가 없는 상태에서는 '옵션 수정하기' 사용 불가 |
| P8 | F1~F7 전체 (세션 상태 전반, 발산 채택) | A1·A2 (동일 사용자가 여러 탭·창에서 동시에 사용하는 경우) | 동일 사용자가 여러 탭·창에서 각각 다른 카테고리·입력으로 세션을 동시에 진행 중일 때 | (발산 채택, 구체적 정책 미정) 어느 탭·창의 상태가 최종으로 반영되는지 [?] | [?] |
| P9 | F2~F4 (목적 축 적용 범위) | A1·A2 모두 동일, 로그인·권한 제한 없음 | 자연어 입력 단계(F2)에서 목적 신호를 판별한 뒤, 옵션 확인(F3) 및 최종 생성(F4) 단계까지 적용 | 입력 문장에 '보고·전달·공유·결과·현황·공지' 계열 표현이 있으면 좌(정보 전달/보고하기), '요청·승인·검토·도와주세요·협조 부탁' 계열 표현이 있으면 우(요청/승인받기)로 분류한다. 양쪽이 모두 없으면 기본값은 좌(정보 전달/보고하기)로 적용한다. 목적 축은 최종 프롬프트의 문체·호칭·요청 구조에 반영되며, 사용자가 확인 화면에서 수정할 수 있다. | 목적 축이 비어 있으면 기본값 좌를 적용하고, 요청형 표현이 명확한 문장은 우로 보정한다. 사용자가 수정하길 원하면 확인 화면에서 목적 값을 재선택할 수 있어야 한다. |
| P10 | F2~F4 전체 (긴급도 축 제외) | A1·A2 모두 동일, 로그인·권한 제한 없음 | 각 단계에서 사용자 입력을 분석할 때 | 긴급도 축은 설계에서 제외한다. 사용자 입력의 '긴급·우선순위·시급' 표현은 결과 생성 규칙에 반영하지 않는다. 목적·대상·문서유형으로 충분히 서비스 요구를 표현할 수 있으므로, 긴급도 값은 별도 데이터 모델에도 추가하지 않는다. | 입력에 긴급도 표현이 있어도 별도 안내나 우선순위 규칙을 적용하지 않고 기본 흐름대로 처리한다. |
| P11 | F2~F4 전체 (축 충돌 처리) | A1·A2 모두 동일, 로그인·권한 제한 없음 | 카테고리별 입력 분석 단계(F2)와 옵션 확인 단계(F3)에서 여러 축이 동시에 설정된 경우 | 일반 조합은 우선순위 규칙으로 처리한다: 카테고리 > 대상 > 문서유형 > 목적. 안전·품질·저작권·개인정보 관련 예외는 별도로 우선 처리하며, 이 경우 안전 규칙이 가장 우선하고, 품질·저작권 안내는 부가 규칙으로 적용한다. 전체 축에 대해 일괄적인 충돌 매트릭스를 만들지 않고, 실제 위험 조합만 예외로 관리한다. | 충돌이 발생하는 조합은 안전 문구·사용 제한·정보 안내를 우선 표시하고, 그 외 조합은 기본 흐름대로 처리한다. |

## 상태값
카테고리 선택 중(F1) → 자연어 입력 중(F2) → 분석 중(F2→F3 전이, '분석' 버튼) → 옵션 확인·수정 중(F3) → 프롬프트 생성 중(F3→F4 전이, '프롬프트 생성' 버튼) → 생성 완료(F4)
· F6(카테고리 재선택)은 옵션 확인·수정 중 → 카테고리 선택 중으로 되돌아가는 전이
· F7(옵션 수정하기)은 생성 완료 → 옵션 확인·수정 중으로 되돌아가는 전이

## 서비스 정책
| SP | 한 줄 |
|---|---|
| SP1 | [?] 팀이 아직 고려하지 않기로 함 (로그인 필요 여부·데이터 보관 기간·이용 연령 제한·유료/무료 여부 등 서비스 전체 정책 미정) |
| SP2 | 본 서비스는 일반 범용 문서 작성·사진 생성용 프롬프트를 기본으로 하며, 안전·품질·저작권과 같이 실제 위험이 있는 조합만 예외적으로 별도 규칙을 적용한다. 전체 축 조합에 일괄 충돌 매트릭스를 적용하지 않는다. |

## 검토했으나 제외
| 후보 | 출처(누구 초안 · [제안]) | 제외 이유 |
|---|---|---|
| 비로그인·무료 사용자에 대한 일 사용량 제한(예: 하루 N회) | 코치 [제안] · 근거: 일반적인 SaaS 확장 프로그램의 무료 티어 관행 (01-problem.md에 명시된 근거는 아님) | 팀 거절 |
| 서비스 이용 연령 제한 여부 | 코치 [제안] · 근거: 01-problem.md 대상 절 "범용 AI를 업무·일상에 활용하려는 비전공자, 특히 40대 이상"에 연령 하한선 미언급 | 팀 거절 |
